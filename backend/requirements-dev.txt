# ─────────────────────────────────────────────────────────────────────────
# SplitLedger — Development & Test Dependencies
# Install: pip install -r requirements-dev.txt
# (This file extends requirements.txt — install both)
# ─────────────────────────────────────────────────────────────────────────

# Pull in production deps first
-r requirements.txt

# ── Testing ───────────────────────────────────────────────────────────────
pytest==8.3.3                   # Test runner
pytest-cov==5.0.0               # Coverage reporting
pytest-flask==1.3.0             # Flask test client helpers

# ── Pretty test runner (utils/test_run.py) ────────────────────────────────
rich==13.9.4                    # Terminal UI: panels, tables, progress bars

# ── Dev utilities ─────────────────────────────────────────────────────────
flask-shell-ipython==0.5.1      # IPython in `flask shell` (optional but nice)

